using Microsoft.AspNetCore.Authentication;

namespace CVGHMI.Models
{
    public class AuthTest
    {

        //public string GetOrganizationUnit(string adid)
        //{
        //    using (PrincipalContext context = new PrincipalContext(ContextType.Domain))
        //    {
        //        UserPrincipal user = UserPrincipal.FindByIdentity(context, adid);
        //        if (user != null)
        //        {
        //            // Retrieve and return the user's organizational unit
        //            return user.DistinguishedName; // This might need parsing for the OU
        //        }
        //    }
        //    return null;
        //}
    }
}
